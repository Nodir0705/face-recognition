<?xml version="1.0" encoding="utf-8"?>
<!-- Outer table card: white, uniform 12dp radius (so the default BACKGROUND
     outline clips child rows cleanly), 1dp hairline border. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="#E5E3DC" />
</shape>
